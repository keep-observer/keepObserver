


//初始化
export var _init = function(){
	//验证必要数据
	//阿里云密匙对和SLS服务器地址
	// var { AccessKeyID,AccessKeySecret,SLSPoint } = this.$report_config
 	// if(!AccessKeyID || !AccessKeySecret || !SLSPoint){
 	// 	this.$devError('[keep-observer] aliyunSLS-server config params :AccessKeyID and AccessKeySecre or SLSPoin is not undefined')
 	// 	return false;
 	// }
 	


 	this.pullLog();
}

